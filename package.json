{
  "name": "cafe-tijdloos-mockup",
  "private": true,
  "version": "1.0.0",
  "description": "Website Café Tijdloos (statische HTML, geen framework)",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node dev.mjs",
    "deploy": "npm run build && npx wrangler pages deploy"
  }
}
